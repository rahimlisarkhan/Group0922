/**
 * fileMock.cjs
 *
 * A stub for static assets (CSS, images, SVGs).
 * When Jest sees an import like:
 *   import styles from './App.css'
 *   import logo from './logo.svg'
 * ...it maps them here and returns an empty string instead of crashing.
 */
module.exports = '';
